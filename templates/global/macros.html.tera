{% macro render_posts(posts, type="text") %}
    {% for post in posts %}
        <div class="post">
            <h2>{{ post.title }}</h2>
            <img src='{{ post.image }}'>
        </div>     
    {% endfor %}
{% endmacro render_posts %}